{
  "name": "agentlas-company",
  "private": true,
  "version": "0.0.0",
  "description": "AI company OS — OAuth-only local model seats, capability switches, hash-chained ledger",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "company": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "company": "node dist/cli.js",
    "test": "vitest --run",
    "gate:apikey": "node scripts/gates/no-api-keys.mjs",
    "gate:flags": "node scripts/gates/no-danger-flags.mjs",
    "gate:liveness": "node scripts/gates/no-liveness-probe.mjs",
    "gates": "npm run gate:apikey && npm run gate:flags && npm run gate:liveness",
    "verify": "npm run gates && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
